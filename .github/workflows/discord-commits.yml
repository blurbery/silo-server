name: Discord Commit Feed

on:
  push:
    branches:
      - main

jobs:
  notify:
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-discord-commits.yml
    with:
      thread_id: ${{ vars.DISCORD_COMMITS_THREAD_ID }}
    secrets:
      webhook_url: ${{ secrets.DISCORD_COMMITS_WEBHOOK_URL || secrets.DISCORD_WEBHOOK_URL }}
